for (int start = 0; start < n; start++) {
        for (int end = start; end < n; end++) {
            // print subarray from start to end
            for (int i = start; i <= end; i++) {
                cout << arr[i] << " ";
            }
            cout << endl;  // new line for next subarray
        }
    }


    //substring
    for(int i = 0; i < n; i++){
    for(int j = i; j < n; j++){
        cout << s.substr(i, j-i+1) << endl;
    }
}
